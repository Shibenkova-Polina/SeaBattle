create table data_base
(
    id int not null primary key,
    Player text not null,
    CellsStates text not null,
    LineNumber text not null
);

create table data_base_2
(
    id int not null primary key,
    Player text not null,
    Data text not null
);